{
  "name": "phishingguard-extension",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npm run clean && vite build && node scripts/build-sw.mjs && node scripts/copy-static.mjs",
    "clean": "rimraf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.275",
    "@types/node": "^20.14.12",
    "esbuild": "^0.25.9",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4",
    "vite": "^7.1.3"
  }
}
